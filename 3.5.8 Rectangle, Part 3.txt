user_length = int(input("What should the Length be?"))
user_width = int(input("What should the Width be?"))


length = user_length
width = user_width
area = length * width
perimeter = 2 * (length + width)
print ( "area " + str (area))
print ("perimeter " +  str (perimeter))